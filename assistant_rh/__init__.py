"""Assistant RH : chatbot agentique (RAG + tools + middleware + memoire).

Repond aux questions sur le reglement interieur (RAG) et automatise les calculs RH
(conges, anciennete) via des tools deterministes, avec une base employes (BDD) et un
middleware qui adapte la reponse au role (employe / manager).
"""
